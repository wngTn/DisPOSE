_target_: src.models.nets.PoseResNet
njoints: 15
root_idx: 2
# conf_threshold lives in the dataset config when it differs from the
# Python default (0.2). Defining it here would clobber dataset overrides
# because /model:Pose composes ResNet into model.backbone.* AFTER the
# dataset config is merged.
root_heatmap_noise_scale: 1.0
